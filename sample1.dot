digraph G {
  main;
  init;
  parse;
  cleanup;
  printf;
  make;
  execute;
  compare;
  main -> init;
  main -> parse;
  main -> cleanup;
  main -> printf;
  init -> make;
  parse -> execute;
  execute -> make;
  execute -> compare;
  execute -> printf;
}
