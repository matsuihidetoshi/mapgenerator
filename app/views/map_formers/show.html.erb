<h1>Map from <%= @part.title %></h1>
<div><iframe src="<%= @former.path %>" width="100%" height="500px" ></iframe></div>

<div class="blank"></div>
<p class ="return">
	<% if @edit %>
		<%= link_to 'Return', edit_part_path(@part), class: 'btn btn-warning btn-block' %>
	<% else%>
		<%= link_to 'Return', root_path, class: 'btn btn-warning btn-block' %>
	<% end %>
</p>
